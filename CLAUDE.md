Maintain the `README.md` file. Each commit should probably contain a diff on this file.

Commit changes after every unit of work without `Co-Authored-By`.

With every commit, store the corresponding prompt at the end of `PROMPTS.md`.
